{
  "name": "google-analytics-mcp-cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Google Analytics MCP server (GA4 Data API + Admin API) running on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.95",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260715.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.4.0"
  }
}
